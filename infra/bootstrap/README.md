
# requirements
aws-cli

# create-tf-state-bucket.sh 
Script to create a secure S3 bucket suitable for holding terraform state.
This script only needs to be run once if a suitable S3 bucket does not already exist.
